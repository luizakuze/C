#include <stdio.h>

int main()
{

    // Definindo Variaveis
    char letra = 'x';

    // Condicional Simples
    if (letra == 'x')
        printf("\n A letra eh X.");

    // Codigo da letra
    printf("\nCodigo da letra = %d", letra);

    if (letra == 120)
        printf("\n A letra eh X.");

    return 0;
}
